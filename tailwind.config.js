/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: false, // ❌ disable system or class-based dark mode
  theme: {
    extend: {
      fontFamily: {
        poppins: ['var(--font-poppins)', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
